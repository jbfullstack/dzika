# === DZIKA — Production Environment ===
# Copie ce fichier en .env et remplis les valeurs.

# Domaine public (utilisé par Caddy pour le HTTPS automatique)
DOMAIN=dzika.music

# Mot de passe PostgreSQL (choisis quelque chose de fort)
DB_PASSWORD=change-me-strong-password

# Secret d'authentification (génère avec : openssl rand -base64 32)
AUTH_SECRET=generate-me

# Admin login
ADMIN_EMAIL=admin@dzika.com
ADMIN_PASSWORD=change-me

# Vercel Blob token (pour les uploads audio/images)
# Crée un store gratuit sur https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=
